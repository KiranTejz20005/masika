# Copy this file to .env and fill in your values. Never commit .env to version control.
# Backend reads all config from environment (see config.py and app.py).

# NVIDIA API (OpenAI-compatible) – get your key: https://build.nvidia.com/
NVIDIA_BASE_URL=https://integrate.api.nvidia.com/v1
NVIDIA_API_KEY=your-nvidia-api-key-here
NVIDIA_MODEL=stepfun-ai/step-3.5-flash

# Optional generation settings
NVIDIA_TEMPERATURE=1
NVIDIA_TOP_P=0.9
NVIDIA_MAX_TOKENS=16384
