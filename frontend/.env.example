# Copy this file to .env and fill in your values. Never commit .env to version control.
# All secrets and API keys must live only in .env (not in code).

# Supabase configuration (required for auth and data)
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Optional: for deep links / redirects
# SUPABASE_REDIRECT_URL=myapp://login-callback

# ═══════════════════════════════════════════════════════════════
# Analysis API (remote HTTP API only – no local backend)
# ═══════════════════════════════════════════════════════════════
# Required. Base URL of your analysis API (no trailing slash).
#   GET  /health  → { "status": "ok" }
#   POST /predict → body: { "features": [12], optional "input_data": {} }
#                   response: { "prediction", "probabilities", optional "report" }
# ML_BACKEND_URL = full base URL of your API (no trailing slash)
# ML_PREDICT_PATH=/predict
# ML_API_KEY=your-api-key-here
#   Real device / production: https://your-api.com
#   Android emulator (API on same PC): http://10.0.2.2:5000
#   iOS simulator: http://localhost:5000
#
# ML_PREDICT_PATH = optional; endpoint path (default /predict). Use if your API uses e.g. /api/predict
# ML_API_KEY = optional; if your API requires auth, set this (sent as Bearer token)
#
#
ML_BACKEND_URL=https://your-api-base-url.com

# Optional. If your API requires auth, set this (sent as Bearer token).
# ML_API_KEY=your-api-key
